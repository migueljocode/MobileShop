@page
@model IndexModel
@{
    ViewData["Title"] = "Dashboard";
}

<div class="text-center">
    <h1 class="display-4">MobileShop</h1>
    <p class="lead">Inventory &amp; sales dashboard</p>
</div>

<div class="row mb-4 g-3">
    <div class="col-md-3">
        <div class="card text-bg-success h-100">
            <div class="card-body text-center">
                <h2 class="card-title">@Model.Stock.PhonesInStock</h2>
                <p class="card-text">Phones in stock</p>
            </div>
        </div>
    </div>
    <div class="col-md-3">
        <div class="card text-bg-warning h-100">
            <div class="card-body text-center">
                <h2 class="card-title">@Model.Stock.PhonesSecondHand</h2>
                <p class="card-text">Second-hand phones</p>
            </div>
        </div>
    </div>
    <div class="col-md-3">
        <div class="card text-bg-info h-100">
            <div class="card-body text-center">
                <h2 class="card-title">@Model.Stock.PhonesSecondHandAvailable</h2>
                <p class="card-text">Second-hand available</p>
            </div>
        </div>
    </div>
    <div class="col-md-3">
        <div class="card text-bg-primary h-100">
            <div class="card-body text-center">
                <h2 class="card-title">@Model.Stock.AppleIdsInStock</h2>
                <p class="card-text">Apple IDs in stock</p>
            </div>
        </div>
    </div>
</div>

<h2>Recent transactions</h2>
@if (Model.RecentTransactions.Count == 0)
{
    <p class="text-muted">No transactions yet.</p>
}
else
{
    <div class="row g-3">
        @foreach (var transaction in Model.RecentTransactions)
        {
            <div class="col-md-6 col-lg-4">
                <div class="card h-100">
                    <div class="card-body">
                        <h5 class="card-title">@transaction.ProductLabel</h5>
                        <p class="card-text mb-1">@transaction.Date.ToString("yyyy-MM-dd HH:mm")</p>
                    <span class="badge @(transaction.Direction == TransactionDirection.Buy ? "text-bg-primary" : "text-bg-dark")">
                        @transaction.Direction
                    </span>
                        <p class="card-text mt-2 mb-0">Finished price: @transaction.FinishedPrice.ToString("N0")</p>
                        <p class="card-text mb-0">@transaction.PartyLabel</p>
                    </div>
                </div>
            </div>
        }
    </div>
}
